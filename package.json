{
    "name": "jsonvar",
    "version": "1.0.1",
    "description": "JSONV, or Variable JSON, is a specialized JSON-like format that allows for the inclusion of variables within JSON objects, making it ideal for dynamic data handling and configuration.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "npx tsc"
    },
    "author": "Karan Saklani",
    "license": "ISC",
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^20.5.9",
        "typescript": "^5.2.2",
        "node": "^20.5.1"
    }
}
